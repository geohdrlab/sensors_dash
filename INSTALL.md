# Installation

Run `./scripts/install.sh` from the repository root. It creates `backend/venv` and installs only the Python dependencies listed in `backend/requirements.txt`.

Start the service with `./scripts/start.sh`. Configuration is supplied through the `AIR_*` environment variables documented in the README.
